[flags]
avg_cpu = true
temperature_type = "c"
rate = 1000
left_legend = true
current_usage = true
group_processes = true
battery = true

[styles.cpu]
all = "#{{primary}}"
avg = "#{{secondary}}"

[styles.memory]
ram = "#{{primary}}"
swap = "#{{secondary}}"

[styles.network]
rx = "#{{primary}}"
tx = "#{{secondary}}"

[styles.battery]
high = "#{{primary}}"
medium = "#{{tertiary}}"
low = "#{{error}}"

[styles.tables]
headers = { color = "#{{primary}}", bold = true }

[styles.graphs]
graph_color = "#{{outline}}"
